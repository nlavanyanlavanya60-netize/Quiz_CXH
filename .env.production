# Empty = use same-origin relative path /api (Single-project Vercel deployment)
# If using a separate backend (e.g. Render), set VITE_API_BASE_URL here or in Vercel Environment Variables
VITE_API_BASE_URL=
