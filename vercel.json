{
  "buildCommand": "cd contestant-frontend && npm install && npm run build",
  "outputDirectory": "contestant-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
